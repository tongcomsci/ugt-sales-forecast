# Optional AI integration.
GEMINI_API_KEY=""

# Microsoft SQL Server connection used by Prisma.
# Format: sqlserver://HOST:1433;database=DB_NAME;user=USER;password=PASSWORD;encrypt=true;trustServerCertificate=true
DATABASE_URL="sqlserver://HOST:1433;database=DB_NAME;user=USER;password=YOUR_PASSWORD;encrypt=true;trustServerCertificate=true"

# Express server and public application location.
# Single deployment path; Nylon vs UFA is selected with ?mode=nylon|ufa (default nylon).
API_PORT=3001
HOST_PORT=3000
APP_BASE_PATH=/ugt-sales-forecast
APP_BASE_URL=https://ugtweb.ube.co.th

# Production authentication. Never enable DEV_AUTH_BYPASS in production.
DEV_AUTH_BYPASS=false
SESSION_SECRET="replace-with-at-least-32-random-characters"

# Keycloak OIDC configuration for realm ugt.
KEYCLOAK_ISSUER="https://ugtweb.ube.co.th/keycloak/realms/ugt"
KEYCLOAK_CLIENT_ID="ugt-sales-forecast"
# Set only when the Keycloak client is confidential. Keep server-side only.
KEYCLOAK_CLIENT_SECRET=""

# Read Registration and Actual data from the local performance snapshot.
USE_LOCAL_SNAPSHOT=true

# Diff plan email notifications (disabled by default).
OVERPLAN_EMAIL_ENABLED=false

# Forecast change email notifications sent on commit save.
# Enable to send one combined CC email to ticked recipients in Manage Email.
FORECAST_EMAIL_ENABLED=false

SMTP_HOST=vcs.ube-ind.co.jp
SMTP_PORT=25
SMTP_SECURE=false
SMTP_FROM=noreply@ube.co.th

# Fully-qualified HR employee view (linked server). Used to sync hr_employee_cache.
HR_EMPLOYEE_VIEW=thrygsd002.ICTPortal_PRD.dbo.vwHR_SC_Employee

# Fully-qualified SAP customer master view (linked server). Used to sync customer_master_cache.
CUSTOMER_MASTER_VIEW=thrygsd002.SAPStaging_Live.dbo.VW_Customer_master

# Actual sales source for CPL Actual sync (sum(Net Amount USD) / sum(Order Qty_TON) by delivery month).
ACTUAL_SALES_VIEW=dbo.MKT_SALEReport_ZSDR001_UGT_CRM_NYL_1

# Fully-qualified inventory view (linked server). Read on the main DATABASE_URL
# connection, so no separate credentials are needed.
INVENTORY_VIEW_NAME=thrygsd002.UBE_DW.dbo.MKT_NYL_Current_INV
